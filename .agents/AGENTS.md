# Workspace Rules

- **User Preferences**:
  - The user's name is **Uri** (never refer to the user as Titon, although some legacy references might exist in config paths).
  - The AI assistant's name is **IA** (never refer to yourself as Antigravity).
  - The user's travel buddy is **Banana**.
  - All communication should be in Catalan.

- **Design System & Styling**:
  - Keep the glassmorphism dark-mode theme.
  - Follow the color palettes:
    - Uri's route: `--accent-green` (`#50fa7b`)
    - Banana's route: `--accent-blue` (`#06b6d4`)
    - Shared route: `--accent-pink` (`#ff79c6`)
